# 环境配置说明

本文记录 ZeroTrace Engine 预期使用的本地开发环境。

## 支持平台

ZeroTrace Engine 主要面向 Windows 开发。

项目核心行为是本地优先，并且以文件系统操作为中心。部分功能，尤其是注册表检查和系统回收站行为，属于 Windows 专用能力。

## Python 环境

优先使用已有虚拟环境。除非有明确原因，不要新建虚拟环境。

Windows：

```powershell
~\.virtualenvs\venv\Scripts\python.exe
```

Linux/macOS，如果只用于非 Windows 检查：

```bash
~/.virtualenvs/venv/bin/python
```

## 创建虚拟环境

只有在 `~\.virtualenvs\venv` 尚不存在时，才需要执行本节步骤。

Windows PowerShell：

```powershell
New-Item -ItemType Directory -Force -Path "$HOME\.virtualenvs"
python -m venv "$HOME\.virtualenvs\venv"
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install --upgrade pip
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install -r requirements.txt
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install -r requirements-dev.txt
```

创建后确认环境：

```powershell
& "$HOME\.virtualenvs\venv\Scripts\python.exe" --version
```

Linux/macOS：

```bash
mkdir -p ~/.virtualenvs
python3 -m venv ~/.virtualenvs/venv
~/.virtualenvs/venv/bin/python -m pip install --upgrade pip
~/.virtualenvs/venv/bin/python -m pip install -r requirements.txt
~/.virtualenvs/venv/bin/python -m pip install -r requirements-dev.txt
```

## 依赖

运行时依赖记录在：

```text
requirements.txt
```

开发和测试依赖记录在：

```text
requirements-dev.txt
```

使用已有虚拟环境安装或更新依赖：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m pip install -r requirements.txt
~\.virtualenvs\venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

## 启动应用

标准本地启动命令：

```powershell
.\start.ps1
```

等价的直接命令：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

启动后访问：

```text
http://127.0.0.1:8000
```

## 运行测试

使用项目测试脚本：

```powershell
.\test.ps1
```

等价的直接命令：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m pytest -q
```

`pytest.ini` 会禁用 pytest 的 cache provider，避免测试缓存进入项目常规缓存流程。

## 运行时数据

以下目录属于运行时状态，不是源代码：

```text
data/
logs/
ZeroTraceRecycle/
ZeroTraceRegistryRecycle/
.test-tmp/
```

这些目录可能包含本地扫描结果、审计日志、回收记录、注册表备份或隔离测试数据。

不要把运行时数据当作可移植源码。除非任务明确要求，并且已经理解安全影响，否则不要删除或重写这些目录。

## 前端环境

前端是位于以下目录中的普通 HTML、CSS 和 JavaScript ES modules：

```text
static/
```

正常开发不需要前端构建步骤，也不需要前端包管理器。

## 安全注意事项

- 文件和注册表操作应保持本地执行。
- 不要绕过 service/orchestration 层执行破坏性操作。
- 优先使用 `ZeroTraceRecycle/` 和注册表备份文件提供的可回滚流程。
- 测试应与真实用户文件和真实系统清理目标隔离。
