# 環境設定ガイド

このドキュメントは、ZeroTrace Engine で想定しているローカル開発環境をまとめたものです。

## 対応プラットフォーム

ZeroTrace Engine は主に Windows 向けに開発されています。

中核となる動作はローカル優先で、ファイルシステム操作を中心にしています。特にレジストリ検査やシステムのごみ箱との連携は Windows 固有の機能です。

## Python 環境

既存の仮想環境を使用してください。明確な理由がない限り、新しい仮想環境は作成しません。

Windows：

```powershell
~\.virtualenvs\venv\Scripts\python.exe
```

Linux/macOS を Windows 以外の確認に使う場合：

```bash
~/.virtualenvs/venv/bin/python
```

## 仮想環境の作成

この手順は、`~\.virtualenvs\venv` がまだ存在しない場合だけ実行してください。

Windows PowerShell：

```powershell
New-Item -ItemType Directory -Force -Path "$HOME\.virtualenvs"
python -m venv "$HOME\.virtualenvs\venv"
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install --upgrade pip
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install -r requirements.txt
& "$HOME\.virtualenvs\venv\Scripts\python.exe" -m pip install -r requirements-dev.txt
```

作成後、環境を確認します：

```powershell
& "$HOME\.virtualenvs\venv\Scripts\python.exe" --version
```

Linux/macOS：

```bash
mkdir -p ~/.virtualenvs
python3 -m venv ~/.virtualenvs/venv
~/.virtualenvs/venv/bin/python -m pip install --upgrade pip
~/.virtualenvs/venv/bin/python -m pip install -r requirements.txt
~/.virtualenvs/venv/bin/python -m pip install -r requirements-dev.txt
```

## 依存関係

実行時の依存関係は次のファイルに記載されています：

```text
requirements.txt
```

開発およびテスト用の依存関係は次のファイルに記載されています：

```text
requirements-dev.txt
```

既存の仮想環境を使って依存関係をインストールまたは更新します：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m pip install -r requirements.txt
~\.virtualenvs\venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

## アプリケーションの起動

標準のローカル起動コマンド：

```powershell
.\start.ps1
```

同等の直接実行コマンド：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

起動後、次の URL を開きます：

```text
http://127.0.0.1:8000
```

## テストの実行

プロジェクトのテストスクリプトを使用します：

```powershell
.\test.ps1
```

同等の直接実行コマンド：

```powershell
~\.virtualenvs\venv\Scripts\python.exe -m pytest -q
```

`pytest.ini` では pytest の cache provider を無効化しており、テスト成果物が通常のプロジェクトキャッシュに混ざらないようにしています。

## ランタイムデータ

次のディレクトリはランタイム状態であり、ソースコードではありません：

```text
data/
logs/
ZeroTraceRecycle/
ZeroTraceRegistryRecycle/
.test-tmp/
```

これらのディレクトリには、ローカルのスキャン結果、監査ログ、ごみ箱レコード、レジストリバックアップ、隔離されたテストデータが含まれる場合があります。

ランタイムデータを移植可能なソースファイルとして扱わないでください。タスクで明示され、安全上の影響を理解している場合を除き、これらのディレクトリを削除または上書きしないでください。

## フロントエンド環境

フロントエンドは、次のディレクトリにある通常の HTML、CSS、JavaScript ES modules です：

```text
static/
```

通常の開発では、フロントエンドのビルド手順やパッケージマネージャーは不要です。

## 安全上の注意

- ファイル操作とレジストリ操作はローカルで完結させます。
- 破壊的な操作で service/orchestration 層を迂回しないでください。
- `ZeroTraceRecycle/` とレジストリバックアップファイルによる復元可能な流れを優先してください。
- テストは実際のユーザーファイルや実際のシステムクリーンアップ対象から隔離してください。
